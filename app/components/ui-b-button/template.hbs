<button type="button" class="btn btn-{{if data.props.style data.props.style}}" {{action 'click'}}>
    {{data.props.title}}

    {{#each children as |component|}}
      {{component component.type data=component}}
    {{/each}}

</button>