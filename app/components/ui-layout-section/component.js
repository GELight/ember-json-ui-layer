import uiBase from './../ui-base/component';

export default uiBase.extend({

  /**
   * Change the tag name of the ember-view element to "section"
   * 
   * @property tagName
   * @default div
   */
  tagName: 'section'

});
